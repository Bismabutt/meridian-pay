apiVersion: v1
kind: Service
metadata:
  name: fraud-service
  namespace: meridian-app
  labels:
    app: fraud-service
spec:
  type: ClusterIP
  selector:
    app: fraud-service
  ports:
    - name: http
      port: 8006
      targetPort: http