apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: meridian-money
  labels:
    app: payment-service
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8004
      targetPort: http