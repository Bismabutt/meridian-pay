apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: meridian-app
  labels:
    app: notification-service
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8008
      targetPort: http